/* Bucle: while */

/* iterador */ 
/* index = 0; 

while (index < 5) {
    console.log('Sirve');
    index = index + 2;
} */

/* Crear un algoritmo: 
    pintar los numeros del 0 - 100
    contando de 10 en 10 */
    
var index = 0;
while (index <= 100) {
    console.log(index);
    index = index + 10;
}